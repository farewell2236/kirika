name: Update SP12 data

on:
  workflow_dispatch:
  schedule:
    - cron: "17 19 * * *"

permissions:
  contents: write

concurrency:
  group: update-sp12-data
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install parser
        run: python -m pip install --disable-pip-version-check requests beautifulsoup4

      - name: Fetch displayed SP12 ranks
        run: python scripts/update_songs.py

      - name: Commit updated data
        run: |
          if git diff --quiet -- data/sp12.json; then
            echo "No data change"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/sp12.json
          git commit -m "Update SP12 displayed ranks"
          git pull --rebase origin main
          git push origin HEAD:main
